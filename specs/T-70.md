# T-70 — Restore stranded XML summaries to their functions

> Frozen spec. Stop and report if this contract cannot be followed; do not improvise.

## Goal

Correct the remaining misplaced summaries without changing executable code. Account for all four reported sites using git history.

## Context

Base: main at 8911c50, task branch task/T-70-doc-comments. Verify git log before editing. Follow CLAUDE.md. The root checkout belongs to other work; use the worker worktree only.

Provenance, established before editing:
- SecretScanner.cs: git log -S 'build01 / sql02' identifies db02d44, where the block directly documents IsListOfPlainNames(string[] lines, int i). Today it precedes IsPairOrRemark instead. Classification: MOVE.
- TaskCommands.cs: git log -S 'Why `task attended`' identifies 2544f5c, where the block documents AttendedRefusal. Its current bool reasonSupplied signature has an expanded comment that must be preserved verbatim. Today that comment precedes LandedHoldWarning. Classification: MOVE.
- TaskService.cs: git log -S 'first non-blank line' identifies 872c495 and c36bc65. In c36bc65^ the current content-based comment directly precedes FirstHeadingTaskId(string content). Today it precedes SpecNeeds. Classification: MOVE.
- KitCommands.cs: ac2e961 documents WriteSection with the old single-line summary; 1a8248a shows both summaries on its renamed SectionDocument. They describe the SAME function: consolidation, not a move. T-55 moved the implementation to Infrastructure/InstallTransaction.cs (6a4ba23), then integrated T-56 in d7afb698, combining both meanings into one summary. Current main already has that correct combined block at SectionDocument. Classification: ALREADY CONSOLIDATED; leave both kit files unchanged.

## Non-goals

No runtime, signature, formatting, build policy, warning settings, test framework or dependency changes. No GenerateDocumentationFile change. No new tests for these comment-only moves. T-65's Collision correction stays intact.

## Design

Move the entire existing XML block, byte-for-byte except its position, in each of the three files. Place it immediately above its rightful declaration, retaining ordinary blank-line separation between members. Preserve the existing summary on the function losing the unrelated block. Never attach these comments based merely on plausible wording.

## Units of work

### Unit A — Three comment moves
- Files: src/Muthur.Core/SecretScanner.cs; src/Muthur.Cli/Commands/TaskCommands.cs; src/Muthur.Server/Services/TaskService.cs.
- Move build01 block to IsListOfPlainNames; attended refusal block to AttendedRefusal; heading block to FirstHeadingTaskId.
- Depends on: nothing.
- Acceptance: exactly these three product files change, only XML comment location changes; each target has one correct summary; existing IsPairOrRemark, LandedHoldWarning and SpecNeeds summaries remain. InstallTransaction.SectionDocument retains its one combined summary.
- Commit changes on the assigned worker branch, never push or merge. Report branch/head, files, commands and results, any deviations.

## Verification

All checks are headless. From the task/worker worktree in PowerShell:

```powershell
$env:MUTHUR_HOME = Join-Path $PWD 'artifacts/t70-scratch-home'
$env:MUTHUR_URL = 'http://127.0.0.1:17470'
dotnet build Muthur.slnx --disable-build-servers -m:1
dotnet test Muthur.slnx --no-build -m:1 --logger 'console;verbosity=normal' --blame-hang-timeout 2m
git diff --check
```

Require exit 0 for build/tests; record totals. Commands must run deterministically with bounded timeout; write large output to logs, do not continually poll. No scratch server is needed. Dispose any own scratch processes before reporting. The orchestrator reruns build/tests itself after integrating.

Review the diff against the frozen base to ensure executable content is unchanged. Scan src/**/*.cs for adjacent XML summaries with regex `(?m)</summary>\s*\r?\n\s*///\s*<summary>` and require no matches. Also review the four named declarations and their summaries explicitly, and confirm T-65 Collision retains its own summary. These source checks are the end-to-end acceptance for a documentation-only change.

## Out of scope / follow-ups

T-91 records the separate decision about XML documentation diagnostics under warnings-as-errors. It is not a prerequisite for these repairs.
