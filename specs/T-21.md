# T-21 — Which inbound channel is next

> Frozen spec. Implement exactly the founder decision; stop if it cannot be followed.

## Goal
Record the founder-approved decision to defer new inbound channels until the first external project exists, so future sessions do not reopen the settled decision.

## Context
Founder request 26 for T-21 was answered on 2026-09-19 (ledger event 2318): "None yet - close T-21 as decided-and-deferred: no channel is built until the first external project exists, and the decision reduces to Azure DevOps if that is the monorepo, calendar if it is personal. I record that in PLAN.md section 11 so the next session does not re-open it. My recommendation."
The first external project itself remains undecided. This is a documentation decision task, not an adapter implementation.
Base: task/T-21-next-channel, created from main at 12c5a06. Verify the committed spec and base using git log before editing.

## Non-goals
No product code, channel, authentication, forge integration, schema, or new task creation. Do not choose the external project or imply either candidate is implemented.

## Design
In docs/PLAN.md section 11, append the following paragraph to numbered item 1 (indented four spaces so it stays within that item), preserving all existing text:

**Next inbound channel — decided and deferred.** The founder decided on 2026-09-19 (T-21, request 26): none yet. No new inbound channel will be built until the first external project exists. If that project is the company monorepo, Azure DevOps is next; if it is a personal repository, calendar is next. The first external project remains an open decision. Email and WhatsApp are deferred. This avoids building a half-used adapter before there is a concrete project need; revisit the channel choice when that first external project exists.

## Units of work
### Unit A — Record the decision
- Files: docs/PLAN.md only.
- Does: insert exactly the paragraph above below item 1 in section 11.
- Depends on: nothing; founder decision already recorded.
- Acceptance: paragraph occurs exactly once, under section 11 item 1; original text remains unchanged; git diff --check passes.

## Verification
Run unattended in the task/worker worktree:
```
git diff --check main...HEAD
git diff main...HEAD -- docs/PLAN.md
dotnet build --no-restore
dotnet test --no-restore --no-build
```
Build and tests must exit zero. If missing restore assets, run dotnet restore then repeat build and tests. Use deterministic process waits; capture large build/test output in files for summarization. Orchestrator independently checks the exact paragraph against this spec and the founder answer, and verifies only specs/T-21.md and docs/PLAN.md differ from the base. No browser, server or scratch hub is needed.

## Out of scope / follow-ups
Any future channel implementation waits until the first external project exists. This task creates no adapter tasks and does not reopen founder request 26.
