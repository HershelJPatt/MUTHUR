# T-107 resumption checkpoint — 2026-09-21

Candidate branch: `task/T-107-verification`; product head: `e45cf101f758843ea4330c211858b2ed3b6b0034`.

The claim resumed preserved `fb52a77` Unit B output despite the worker timeout ledger. The remaining checkout identity amendment was already present in the working tree. It was committed as `d7f7243`, registered as the frozen spec, delegated through the implementer tier, reviewed, and fast-forwarded from `worker/T-107-checkout-identity` at `e45cf10`. Worker run `5d18d9363d24422bbabb329aef6e1792` used codex/gpt-6-astra and was committed by the launcher. Only `VerificationEnvironment.cs` and its regression test changed in that unit. Worker build/test attempts were blocked by denied user NuGet-config access; none are counted as passed.

Scope reconciliation: T-95, T-99 and T-117 are done. T-107 has no question/answer changing approved scope. No human-only decision, permission, account, budget, independent validation or installed/full-suite gate was waived. Local implementation was not used. Existing post-landing observation task T-120 depends on T-107; no duplicate follow-up was created.

Independent review covered the complete runner/cache/containment/CLI/test diff and the final normalization correction. Independent `dotnet build` passed with zero warnings and errors. The pilot PowerShell parser reported zero errors. Pinned `scripts/install.ps1 -Destination C:/WorkSrc/MUTHUR/artifacts/107i2 -Ref e45cf101f758843ea4330c211858b2ed3b6b0034` succeeded under a scratch MUTHUR_HOME/URL. These are not approval or full task completion.

The independent `dotnet test -v n` reported an inherited failure in `CapabilityProbeTests.Host_setup_never_runs_configured_helpers(mode: "clean")`: `Probe command execution budget expired.` The stack identifies fixture `Request()` line 29, the initial Git commit, and assertion line 25, before the helper-safety behavior. Exact first evidence: `artifacts/T-107-resume/final-test.log` lines 8642–8656. There is no T-107 diff from integrated main 2e51fd4 in `CapabilityProbeTests.cs` or `CapabilityProcessRunner.cs`. Do not attribute a cause beyond the observed ten-second execution-budget expiry.

T-119 is already in progress and its latest frozen Unit C explicitly covers failures of this helper-safety test during fixture Git commit setup, preserving timeouts and requiring evidence-led diagnosis. This is the applicable existing prerequisite; do not duplicate its repair in T-107 or retry until green. The installed three-run pilot is gated on successful independent checks and was not started on this failed gate.

Evidence directory: `C:/WorkSrc/MUTHUR/.worktrees/T-107/artifacts/T-107-resume`. Build: `final-build.log`; suite: `final-test.log`; result codes: `final-check-results.json`; pinned installation: `final-install.log` and `final-install.exit`; suite summary: `final-test-summary.json`; cleanup audit: `final-cleanup.log`. Utility summaries are advisory; cited original failure lines were verified. The attempted summary of the actively written suite log was refused because the file was open; no cloud summary fallback was used.

Resume after T-119 lands: preserve this branch and spec, integrate the prerequisite on the task branch, review the change, run the required full suite and pinned install, then complete the three actual installed miss/hit/corrupt-rebuild runs plus cancellation/concurrent containment fixtures. The installed pilot and final independent validation are still outstanding. T-120 retains unavailable post-landing real-use measurements. Never submit implemented until every required gate passes and owned scratch processes are cleaned.

Final gate outcome: full suite exited 1 after 14m47.82s; 5,245 passed, 1 failed, 1 skipped (Core 3,742; Data 64; CLI 323; Launch 202 passed/1 failed; Server 914 passed/1 platform skip). The sole failed case is the fixture Git commit budget expiry cited above. The utility summary was truncated and missed that middle-of-log failure; its suggestion that prune-package fallback explains the failure is not accepted. Original project result blocks and failure stack were verified directly. Build exit 0, pinned-install exit 0, parser errors 0. Pilot output root 107p2 was never created, so its sample count is zero.

Cleanup audit completed with 623 examined, 610 deleted, 9 retained logs, 0 held, 4 age-protected, 0 failures. The test/installer/worker/wait/heartbeat commands all completed; no T-107 scratch hub or test/worker process remains. Prior timeout/containment ownership records are cleaned with no running command. Retained candidate installation, intentional worktrees and logs are evidence for resumption, not live processes. No live hub installation or external publication occurred.
