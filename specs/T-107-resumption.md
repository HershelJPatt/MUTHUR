# T-107 resumption checkpoint — 2026-09-21, fourth continuation

Preserved task branch: task/T-107-verification. Current product candidate: 37568eede16d94e71811fb61c1a9c6f6e973aac4. Do not restart implementation or discard the retained pilot evidence.

## Reconciliation and integrated work
T-95, T-99, T-117 and T-119 are landed. T-107 has no question/answer changing approved scope. The resumed 22d4fae checkpoint matched the ledger. Integrated main 547d987 (including T-119 and current T-88 source-only/protected-definition procedure) into the task branch at 816e36d. No founder decision, permissions, budget, accounts, outbound gate, independent validation or full-suite gate was waived. T-120 remains the existing post-landing real-use observation follow-up.

Independent build at 816e36d passed with zero warnings/errors; full `dotnet test -v n` passed 5,250, failed zero, skipped one existing platform case, elapsed 12m02.44s. Project counts: Core 3,742; XmlDocCheck 64; CLI 323; Launch 207; Server 914 plus one skip. Pinned install 107i4 and pilot parser passed. Local utility summary was attempted, first denied the sandbox lock and then refused a busy inference lock; original result blocks were checked directly. This baseline does not substitute for corrected-candidate checks.

## Real installed findings and delegated corrections
Actual installed run 7de270a3-c108-4257-97e3-2623e3ca4a16 at 816e36d, output C:/WorkSrc/MUTHUR/artifacts/107p4/first, found empty vswhere stdout with exit zero, disabling reuse. Five controlled exact-environment probes showed ProgramData alone fixes discovery, ALLUSERSPROFILE alone does not, and SystemDrive is an alternative. Frozen Unit C chose only ProgramData, inherited and hashed, with no permission expansion. Implementer-tier run 457d5c49d05c4856804e69abf062f1d6 produced 3a7ef15, committed by launcher. All three changed files were reviewed and integrated. Independent build passed zero warnings/errors and focused Verification tests passed 67/67. Worker restore/build/tests were sandbox-blocked by user NuGet.Config access and count as zero executed tests.

The pilot was deliberately interrupted by verified supervisor PID/start/executable identity after the cache blocker. Job containment stopped descendants, but old installed recovery refused a test-created descendant junction under its owned scratch. CIM audit found zero remaining pilot processes. Its target was the fixture's kitoutside directory; the tree was retained as a real reproduction. Frozen Unit D distinguishes linked roots/ancestors (still refused) from descendant links (unlink only, nonrecursive, never follow). Implementer-tier run 50fd90201cac445e9efee08a2994b2da produced 37568ee, committed by launcher. Reviewed all six changed files: safe unlink traversal, existing cleanup token forwarding, external read-only sentinel/dangling-junction/recovery tests and docs. Worker checks again executed zero tests due NuGet.Config access; no worker claims were treated as passed. Both new worker worktrees were removed after archiving their logs and reports. Their branches remain.

The failed 107p4 cohort has zero successful samples and is never pilot acceptance. Its original pilot.json records the discovery interruption and old recovery failure. Keep those failures even after repaired cleanup succeeds; do not rewrite them as a successful run.

## Corrected candidate verification
A bounded independent driver, artifacts/T-107-r4/corrected-check.ps1 -Ref 37568eede16d94e71811fb61c1a9c6f6e973aac4, runs build, full suite, pinned install C:/WorkSrc/MUTHUR/artifacts/107i5, then two installed cleanup calls on the preserved 107p4/first output. Parent heartbeats use the live ledger; child processes use isolated TEMP/TMP/GIT_CEILING_DIRECTORIES=artifacts/107t5, scratch home 107h5 and URL loopback port 1. Each stage has a process-tree deadline and captured output. Inspect corrected-*.exit/stdout.log/stderr.log before continuing; a missing exit file is not a pass. At checkpoint preparation, corrected-build passed zero warnings/errors and corrected-test was still running. A later result section supersedes this sentence when present.

All evidence for this continuation is C:/WorkSrc/MUTHUR/.worktrees/T-107/artifacts/T-107-r4. Summarize new large logs with the required local utility before reading full text, then verify cited original evidence. Worker report summaries are advisory. The standard SDK prune-package fallback in successful logs is not a task failure.

## Remaining installed cohort, same pinned identity across resumptions
The frozen spec now explicitly permits exact sequential pilot phases across conductor windows, preserving original execution timestamps and candidate/CLI/spec/recipe identity. It waives no test or installed checks. Prepared scratch QA drivers (not product changes):

- artifacts/T-107-r4/phase-pilot.ps1 uses the committed pilot script's exact helper functions and fixture/finalization block, verifies prior successful evidence and native executable identity, and persists original observations.
- artifacts/T-107-r4/run-phase.ps1 runs one phase with automatic live heartbeats, a 25-minute process-tree bound, fail-fast on disabled provenance, and installed cleanup in finally.
- Run `pwsh -NoProfile -File artifacts/T-107-r4/run-phase.ps1 -Phase first`, then second, then corrupted, then fixtures, sequentially. All use ref 37568eede16d94e71811fb61c1a9c6f6e973aac4, installation 107i5, new cohort root C:/WorkSrc/MUTHUR/artifacts/107p5 and common cache. First requires a new root; later phases refuse identity mismatch. Both driver parsers passed; their execution is still outstanding.
- Each real recipe includes fresh full build/test/smoke. Never count earlier baseline checks as a cohort sample. Each full suite can take about 12 minutes on this host; plan phases to fit the conductor window and checkpoint completed observations rather than restarting the cohort.
- Before accepting a completed phase, inspect its exit, original evidence, cleanup and first/second producer/key match. If an attempt fails or is interrupted, preserve it, recover owned scratch, and diagnose; do not label it a success or overwrite evidence. If candidate identity changes, start a new cohort.
- The final fixtures phase runs real cancellation/concurrent-isolation/hard-kill child checks and writes the required pilot.json with three original observations. Validate that final bundle, actual sample count/window/ref, per-run durations, zero execution model calls, defects and missing post-landing data. No speedup or live-use claim is authorized from these samples.

After all required corrected checks and all pilot phases pass: audit/clean owned compiler/test/scratch processes, retain useful evidence, commit final checkpoint/evidence, `muthur task implemented T-107 --branch task/T-107-verification`, report branch/head/checks and EXIT for the conductor's independent validation/landing. Never push or merge default branch.

## Cleanup already completed
Baseline compiler PID 66876 was identified by exact start ticks and loaded analyzer modules beneath owned 107t4, then stopped after checks completed. Baseline 107t4 and isolated vswhere probe 107probe4 were removed. An initial audit checked only dotnet.exe and missed standalone VBCSCompiler.exe; its later module audit corrected that, and deletion completed. Initial removal errors are not counted as final cleanup success without this later confirmation. Baseline clean-test-temp reported zero remaining test directories/failures. Retained 107i4 installation, failed 107p4 evidence and task worktree are intentional evidence. Corrected 107t5/compiler and real interrupted-output cleanup still need final confirmation from the corrected driver/audit.

## Final results and clean handoff, 2026-09-21
Corrected candidate 37568eede16d94e71811fb61c1a9c6f6e973aac4 passed independent build with zero warnings/errors and the complete `dotnet test -v n`: 5,253 passed, zero failed, one existing platform skip, elapsed 12m02.58s. Counts: Core 3,742; XmlDocCheck 64; CLI 323; Launch 210; Server 914 plus one skip. Corrected build/test/install/recovery/recovery-idempotent exit files are all zero. Pinned installation 107i5 reports exact source 37568ee. Both installed recovery calls returned interrupted with exit zero for the original run, its evidence now has cleanupSucceeded=true, and the formerly retained junction scratch root is absent. The original failed pilot.json remains a failure with zero successful samples.

The required full-suite utility summary completed but was truncated and reported only the final Server project total; the original six project result blocks and zero-warning/error build footer were checked directly. The actual full-suite count is 5,253 passed, not the summary's Server-only 914.

A separate installed one-minute diagnostic at the corrected candidate (run ded6a653-95ce-4a1d-9c70-70d0370a76f1; output C:/WorkSrc/MUTHUR/artifacts/107diag5) confirmed vswhere now returns the BuildTools path. Its intentional deadline expired during content fingerprinting: exit 124, status timeout, testsStarted=false, key=null, cleanupSucceeded=true, no success cache. It is not a successful pilot sample or proof of a complete reusable key. Allow enough time for fingerprinting in the real 107p5 cohort; the 25-minute outer phase budget remains only a hang detector. No 107p5 phase has started.

Final audit identified corrected installation compiler PID 76908 by start ticks and analyzer modules under 107t5, stopped it after all checks/diagnostic completed, and removed 107t5/107h5/107dh5. Earlier 107t4/107probe4 cleanup is complete. CIM audit found no processes for corrected-check.ps1, 107diag5 or 107p4. The two implementer launcher commands, utility commands and wait drivers are complete. Both new worker worktrees were removed after archiving evidence; task branch, installed candidates and evidence bundles are intentional retained artifacts. No owned scratch process remains and no human cleanup is needed.

The conductor's 45-minute session window is ending. Preserve this tested candidate and resume directly with the exact phased pilot commands above; do not spend another session redoing unchanged product checks or recreating implementation. Review the prepared scratch drivers before use; their parsers passed and their actual phase execution is still outstanding. The final checkpoint commit only changes this resumption evidence, not tested product code, the frozen spec, recipe or pilot script. T-107 is not implemented yet because all three real successful pilot phases and the final installed fixture/cohort bundle are still required. No prerequisite blocks progress and no founder decision is needed; release is solely for the bounded conductor continuation, not a dependency workaround.

## Fifth continuation — installed failures and admitted-worker prerequisite, 2026-09-21
This section supersedes the earlier outstanding-pilot instructions. Product remains 37568eede16d94e71811fb61c1a9c6f6e973aac4; no product changes or implementation children were made in this continuation. Prior corrected independent build/full suite/install/recovery remain verified historical results (5253 passed, one platform skip). The previously uncommitted fourth-continuation checkpoint is preserved with this handoff.

The first real 107p5 phase ran from 2026-09-21T17:54:15Z to the recorded evidence end time. Run 1e7fb8cc-0b47-429c-a9d9-438d30e89cc7 established key 5d7bd0d9e59f168a8dda1c81e999de369603dd2804b9199f37c805c275add9e6, passed toolchain probes and build, then failed fresh full tests. Completed project totals were Core 3742 passed; XmlDocCheck 63 passed/1 failed; CLI 322 passed/1 failed; Launch 193 passed/17 failed. Server was still running and has no final total. A deterministic 20-minute test-stage diagnostic deadline stopped the verified PID/start/executable process tree before the outer 25-minute driver kill, allowing partial logs and normal runner cleanup. This is not a full-suite pass. The evidence testsCompleted=true means the child returned here; it must NOT be cited as successful full-suite completion after the recorded external stop.

Output and original logs: C:/WorkSrc/MUTHUR/artifacts/107p5/first. Cohort pilot.json explicitly records failure, zero successful samples, one attempted run, zero model calls in deterministic execution and missing observations. Phase exit is 1. The runner records failed, test exit -1, cleanupSucceeded=true. Ownership cleaned=true, owned scratch absent, no cache directory/entry, no remaining 107p5 worktree registration. Utility summary of the large stdout was attempted successfully but truncated to a tail with intentional fixture failures; original failing test blocks and project totals were inspected. Utility model invocations are separate from runner execution model-call count.

Concrete findings: nonrepository fixtures found enclosing branch task/T-62-brief-file-rules at 13d9d8e because no owned Git discovery ceiling was set; nested capability Git init failed exit 128 after user config exclusion removed core.longpaths=true. Bounded Git reproduction confirms both causes and narrow remedies. Its original outputs are artifacts/T-107-r4/isolation-reproduction.json; diagnostic directory 107diag6 was removed after its resolved absolute path was verified. Frozen Unit E in specs/T-107.md specifies generated owned Git config, stable policy identity and owned ceiling, with exact regression and full-gate requirements. Do not copy user configuration, change permission settings, skip tests or change capability policy.

Before delegation, inspected T-121 and its complete applicable decision: founder request 53 answer/event 7234 permits direct coding ONLY for T-121's already-admitted session to bootstrap full-worker admission. T-121 is still in_progress and the supported installed full-worker admission route is not yet supplied. This does not waive T-107's delegate-only rule. Therefore no Unit E child was launched; T-107 is parked behind T-121 using a ledger dependency, not released to runnable backlog and not sent to the founder for polling. Existing T-95/T-99/T-117/T-119 prerequisites remain landed, and no T-107 scope-changing question exists.

Resume after T-121: verify the landed installed admission route and latest applicable decisions; dispatch only frozen Unit E through the implementer tier; review changes; run required independent build/full suite/pinned installation and NEW three-run cohort plus installed fixtures. Preserve 107p5 failure; do not execute its second/corrupted phases or reuse the old phase scripts unchanged for a new candidate. All successful miss/hit/corrupt-rebuild samples remain outstanding. T-120 retains post-landing observation scope.

Cleanup audit: installed final cleanup completed successfully; original run scratch and diagnostic Git tree absent; CIM audit found no owned 107p5/run-phase/phase-pilot process; the diagnostic deadline process and all local-summary commands exited; no leftover pilot worktree registration. Task branch, installed candidate and evidence bundles are intentional retained artifacts. No scratch process or manual cleanup is left behind. T-107 is NOT implemented and no validation/landing claim is made.
