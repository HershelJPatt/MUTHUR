# T-64 — Verify the server-side brief provenance already landed in T-62

> Frozen spec. If this is wrong or incomplete, stop and report; do not improvise.

## Goal
Verify and record T-64 coverage on current main, retaining the shared strict refusal. Add the missing server-level fake-process regression evidence, without changing product behavior.

## Context
Founder answer to request 37 (ledger event 4309) directs retaining strict refusal, treating T-64 as covered by T-62 after verifying current main and recording evidence, with normal independent validation. No browser override.
Base main: e513f5aa3f0d6b7589e34b97c168ef18af69f55a. T-62 head 13d9d8e is its ancestor. Verify both facts yourself.
Read CLAUDE.md and specs/T-62-brief-file-rules.md. Existing code: Console.razor injects BriefFileReader and reads before Roles.DefineAsync; BriefFileReader uses RepoFile.IsDirtyAsync through constructor-injected IProcessRunner; CLI FileProvenance delegates to the same RepoFile. Muthur.Server must not reference Muthur.Cli. BriefFileReaderTests currently exercise real git, but T-64 explicitly calls for a fakeable server provenance seam, exercised with fakes.

## Non-goals
No product-code changes, new interface layer, schema, endpoint, browser override, provenance display, or altered git-failure/containment semantics. Do not claim universal guarantees beyond current behavior: outside-git and failed initial git probes preserve existing best-effort behavior. No hub commands by workers. No push or merge into main.

## Design
Keep all production files unchanged. Add tests/Muthur.Server.Tests/BriefFileReaderProcessTests.cs, using a private IProcessRunner fake and real temporary files only (no git processes and no actual git repository). Instantiate BriefFileReader with the fake. Dispose every owned temp directory.
The fake records executable, arguments, working directory, cancellation token and timeout and returns queued ProcessResult values matching the existing interface. Unexpected calls fail the test. Cover:
1. Fake inside-worktree=true then modified status: ReadAsync throws MuthurException brief_file_dirty with RuleViolation and named relative file. No ls-files call after dirty status.
2. Fake true, empty status, failed tracked-file lookup: refuse untracked brief with same code.
3. Fake true, empty status, successful tracked-file lookup: return exact file text.
4. Blank input, empty roots, and missing file each fail with existing codes before any process call.
Assert git uses the file directory, literal-pathspec status/tracked-file arguments, ten-second budget, and supplied noncancelled token for applicable probes. These assertions establish the external-process seam and bounded execution; existing real-git tests cover actual git semantics.
Write docs/T-64-provenance-evidence.md with base/head identifiers, request-37 decision, a requirement-to-source-and-test coverage table, exact checks and results, and limitations above. Distinguish tests actually run from inherited T-62 evidence. Explain that the server uses an existing injected process interface rather than a new IFileProvenance abstraction. Identify any uncovered task acceptance requirement precisely and stop if a production change would be required.

## Units of work
### Unit A — Regression evidence for the landed implementation
Files: only tests/Muthur.Server.Tests/BriefFileReaderProcessTests.cs and docs/T-64-provenance-evidence.md.
Depends on: nothing; work from task/T-64-provenance, never a stale T-62 branch.
Acceptance: tests below pass and product-code diff is empty. Commit changes. Report branch, HEAD, files, checks, cleanup, and limitations.

## Verification
No browser, GUI, live hub, or manual step required. Run:
```
dotnet build
dotnet test tests/Muthur.Server.Tests --no-build --filter "FullyQualifiedName~BriefFileReader|FullyQualifiedName~DashboardConsoleRoles" -v minimal
dotnet test tests/Muthur.Launch.Tests --no-build --filter FullyQualifiedName~RepoFile -v minimal
dotnet test tests/Muthur.Cli.Tests --no-build --filter FullyQualifiedName~FileProvenance -v minimal
```
Passing means exit 0 for all commands, zero failures, and no compiler warnings. Existing server tests include an HTTP GET /console with the injected reader. These exercise the console's actual service, shared rules and CLI wrapper without a development hub or browser. Orchestrator independently runs the same checks before submission. Save large outputs to files and summarize before reading. Use deterministic command execution/timeouts, no sleeps in tests. Clean owned scratch processes before submission.

## Out of scope / follow-ups
None planned. Shared rule preservation and duplicate-scope resolution follow the recorded decision; do not manufacture a new product decision.
