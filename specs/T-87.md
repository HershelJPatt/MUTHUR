# T-87 — Claude Agent-tool delegation depends on harness permission mode

> Frozen spec. Documentation-only, authorized by founder request #33 (2026-09-20, ledger 3928), correcting request #28's original diagnosis.

## Goal
Document the observed permission-mode dependency accurately while preserving supported Agent-tool delegation and the deferred real measurement.

## Context
Edit the source kit `kit/claude/skills/muthur-orchestrate.md`, not installed `.claude` files. Current base main is 3ca12c97be026cfbede053009e23fc4c4c94b5f2. Existing native isolation/base guards remain intact. MUTHUR ships no allowlist for native Agent-tool subagents. WorkerCommands.DefaultAllowed belongs to separately launched workers, not native Agent-tool provisioning.

## Non-goals
No product code, tests, permission files, agent definitions, installed skills, permission-mode changes, policy choices, runtime probes, or declaration that delegation is unsupported. T-67's own required Claude probe remains mandatory. This documentation unit's worker/build results are not Claude Agent-tool measurement evidence.

## Design
Add a concise permission-mode guidance subsection to the Claude orchestration source after its existing bullets. State that native Agent-tool success depends on the initiating harness permission mode/approval availability; MUTHUR does not set, record or observe that native mode or ship its allowlist. Unattended denial is an observed configuration outcome, not proof of universal failure. Read-only success and static settings inspection cannot establish build/test/commit capability. Record actual attempted commands and approval/denial outcomes when encountering this gap; report blocked verification honestly and preserve artifacts. Do not widen permissions to make a unit pass. Worker launcher is a separate path and its success does not prove native behavior.

Create `docs/claude-agent-permissions.md` titled "Claude Agent-tool delegation depends on harness permission mode". Link it from the new subsection using `docs/claude-agent-permissions.md` explicitly as a repository-root path (installed skill paths differ). It must correct T-87's original "delegation is dead" headline, retaining that only as an explicitly rejected historical conclusion. Include:
- Source attribution to T-87 founder request #28, 2026-09-19, ledger 2250: five prior real Agent-tool units (three T-59, one T-60, one T-69) built and ran full suites; founder independently reran integrated suites with matching counts. No numerical counts were provided in the decision: do not invent them. T-69 also installed CLI and used scratch hub. Several prior reports had initial NuGet sandbox denials then tool-approved successful retries. These are founder-reported historical evidence, not new measurements.
- T-57's historical reported denials: read-only git worked; dotnet and mutating git denied in that unattended session. Do not generalize its cause/outcome to every session or assert all prompt requests always deny.
- Static separation between native Agent-tool permission inheritance and worker-run allowlist/adapter rendering. T-67 covers shell rendering/rails; do not claim it grants a native allowlist or has landed.
- Founder #33, 2026-09-20, ledger 3928 permits documentation landing ahead of runtime measurement. Post-T-67 native behavior is not yet measured. No permission-provisioning policy is authorized.
- Durable follow-up T-97 (parent T-87), with explicit ledger dependency on T-67 already verified by orchestrator. After T-67 lands it must run one real Claude Agent-tool unit in founder's normal permission mode; record installed revision, harness/version, permission mode, allocated worktree, attempted commands, allow/deny and approval outcomes, build/test counts, commit behavior and artifacts, limitations and independent verification. Preserve historical baseline separately. Codex/worker-run/static-source results cannot substitute. T-67's own required probe is not waived. Bring any allowlist provisioning proposal to founder with this evidence via `muthur ask --kind human`; no agent/hub permission expansion on its own authority.

## Units of work
### Unit A — documentation
- Files: only `kit/claude/skills/muthur-orchestrate.md` and new `docs/claude-agent-permissions.md`.
- Does: the exact documentation above. Do not edit this frozen spec.
- Depends on: nothing; T-97 owns the T-67 prerequisite.
- Acceptance: accurate provenance/time distinctions, intact existing native/launcher guidance, no permission changes, no invented results, full verification below.

## Verification
Run `git diff --check`, `dotnet build`, and `dotnet test` in the allocated worktree. All must pass; capture real results and failures. Orchestrator independently runs the same build/test on integrated head. Review changed-file list against the two-file unit allowlist plus this spec; verify the doc's required evidence and T-97 reference manually. No new runtime test needed for prose. No browser or Claude availability required for T-87. Validator reads `muthur task show T-97` and checks dependsOn includes T-67. All runtime claims are historical attribution or explicit unknowns.

## Out of scope / follow-ups
T-97: real Claude Agent-tool measurement, explicitly depends on landed T-67, followed by founder-owned permission policy decision. Identified existing task, not newly claimed. T-87 remains independently deliverable under #33.
