# T-105 — A2 retry isolation correction

Frozen bounded implementer assignment. This supplements specs/T-105.md; implement only the correction below. Preserve all existing scope, permissions and independent validation gates. Do not merge, rebase, push, change protected definitions or use the hub. The dispatcher supplies the live base branch and full SHA; verify your actual allocated worktree and base before writes. Git metadata may be read-only: leave tracked output for launcher commit if so, without retrying Git writes.

## Defect and required behavior
IntegrationService.ValidateCurrentAsync invalidates a historical candidate when the task's current subject changes, then can return the NEW validated subject to in_progress because the OLD candidate reached ConductorMaxAttempts. Retry budgets are per subject. Once the owner revalidates/resubmits and a new subject receives its independent verdicts, the old candidate must not change that new subject's lifecycle or lease.

Restrict cap-driven ReturnToOwner in ValidateCurrentAsync to the candidate belonging to the task's current subject and current candidate identity. Still invalidate and retain old active candidates when inputs/subject mismatch, and still refuse their updates. Keep cap recovery for the same current subject, lease expiry, target movement, behavioral failure and all existing caller checks. Do not reset historical Attempt fields or discard evidence. A fresh subject receives a new candidate with Attempt=1. No other product refactor.

Add a deterministic API regression to IntegrationServiceTests: set ConductorMaxAttempts=1; independently approve and claim subject A; before that active candidate completes, revalidate/reattach spec/resubmit and independently approve subject B through the normal APIs; reconcile/claim B. Assert A is retained and invalidated, B stays validated with its own verdict, B gets a different candidate at attempt 1, and a late A report is rejected without changing B's state/lease/current candidate. Also preserve existing same-subject cap test. No sleeps.

Candidate registration replay clarification: for an identical already-recorded CandidateAsync payload and still-current inputs, return the existing record without mutation or lease extension even after passed/failed evidence. A differing registration retains integration_candidate_invalid (422); a stale terminal identity retains stale_integration_candidate (409). Never accept a new registration in terminal state, revive invalidated/interrupted/promoted candidates or bypass freshness/ownership checks. Add one terminal identical/different replay regression. This clarifies the already-frozen immutable/idempotent registration contract; no new passing authority.

## Checks and handoff
Use own artifacts/review directory with APPDATA=<scratch>/appdata, DOTNET_CLI_HOME=<scratch>/dotnet and NUGET_PACKAGES=C:/Users/hersh/.nuget/packages. Keep ordinary system TEMP/TMP. Create offline.config containing <configuration><packageSources><clear /></packageSources></configuration>. Run dotnet build --disable-build-servers -p:RestoreConfigFile=<absolute-config> -p:NuGetAudit=false, then dotnet test --no-build --no-restore -v n --filter FullyQualifiedName~IntegrationServiceTests, and git diff --check. Redirect logs and use a deterministic scoped watchdog. No full suite or installed/hub checks in this unit. Report actual WORKTREE/BRANCH/base/head, exact changes and checks/counts, cleanup, limitations; preserve partial work if interrupted. The orchestrator retains final full-suite/installed submission gates.

