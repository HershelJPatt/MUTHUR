# T-94 — Discover headless browser capability before blocking interactive validation

> Frozen spec. Implement this contract; report gaps rather than redesigning it.

## Goal

Provide bounded discovery and execution of installed headless Playwright/Chrome for unattended interactive validation, without mistaking connector absence or fetched HTML for browser capability or interaction evidence.

## Context

Base: main at 8ee5f4d (verify ancestry before building). TaskService.SpecNeeds currently routes every declared need to attended. kit/core/orchestrate.md, validate.md, spec-template.md and briefs/validator.md teach the same assumption. KitInstallTests pins the old wording. Existing T-93 evidence used Node, C:/WorkSrc/MUTHUR/.work/overseer-0919/browser/node_modules/playwright and C:/Program Files/Google/Chrome/Application/chrome.exe. These are discovery hints, not dependencies to vendor or automatically install.

## Non-goals

No package downloads, npm/npx installs, permission changes, sandbox bypass, paid sessions, browser profiles from users, new conductor slots, or schema/API changes. Keep legacy needs: browser attended. No generic capability scheduler. No product UI redesign.

## Design

1. Add scripts/browser-capability.ps1, scripts/browser-capability.cjs, scripts/browser-smoke.cjs and tests/browser-capability.test.cjs. PowerShell is the bounded entry point even when Node is missing. Parameters: -NodePath (default node), -PlaywrightPath (optional absolute module directory), -BrowserPath (optional absolute installed executable), -TimeoutSeconds (default 30, valid 1..120). Optional -Url and -AgentName run the smoke after capability succeeds. Probe emits one JSON object and exit 0 for available, exit 2 for unavailable; unexpected errors are also nonzero and actionable. JSON separates connector (status unknown: caller must inspect its connector), headless (available/unavailable, reason, selected node/module/browser paths), and http (render-only; never interactive). No connector probe is invented. Node absent, module missing, executable missing and launch denied/timeout are distinct reasons. Explicit invalid overrides never silently fall back.
2. Discover only installed tooling: resolve node from PATH, resolve playwright or playwright-core from the script/repository using Node resolution, allow an explicit module path (including an already installed external path); discover standard Windows Chrome/Edge paths or Playwright's already-installed chromium executable. Do not recursively scan disks or invoke package managers. Launch headless with normal browser sandbox/security defaults; never add --no-sandbox or ignore certificate errors. Use a fresh nonpersistent browser context, local data URL button with a click and changed-text assertion to prove capability, close context/browser in finally. Parent PowerShell enforces the total deadline and kills its own process tree on timeout; never terminate unrelated browsers. All argument handling uses argument arrays and supports paths with spaces. No shell concatenation of paths.
3. Smoke mode requires explicit loopback http URL with non-default port (reject 7420, credentials, non-loopback hosts, redirects to other origins); require AgentName. Against an already running isolated scratch installed hub, visit /console, wait for the Blazor connection, fill agent-name, agent-harness=fixture, agent-model=fixture, agent-tier=mastermind, click Register, assert successful registration via the scratch /api/agents response and rendered UI or success feedback. Fail on page errors. The runner does not launch a hub or use live identity/token. The caller owns starting/stopping scratch with per-child MUTHUR_HOME/MUTHUR_URL. Always close owned browser processes, including failures. Print concrete interaction evidence separately from probe JSON if smoke requested. Inspect actual routes and feedback in Console.razor to use exact selectors/API shape.
4. Add one recognized need: headless-browser (case-insensitive after existing markup trimming). TaskService.SpecNeeds skips only this exact need and continues scanning all lines for other needs; every other need remains attended, in any order. Do not clear existing/manual attended reasons when replacing a spec. Update comments/reason text to avoid claiming all unattended sessions lack browsers. Tests cover marker alone, markup/case, mixed declarations both orders, unknown declarations and retained attended reason. No conductor changes: headless is explicitly unattended-capable; its actual prerequisites must be preflighted before builds and before implementation submission.
5. Update kit/core/orchestrate.md, kit/core/validate.md, kit/core/spec-template.md, specs/_TEMPLATE.md, kit/briefs/validator.md, briefs/validator.md, and tests/Muthur.Cli.Tests/KitInstallTests.cs. Teach three distinct paths: HTTP-only assertions, connector-driven UI, installed headless interaction. Connector unavailable means try explicit headless probe under existing permissions. Headless spec declares needs: headless-browser and exact probe/tool paths/interaction commands; needs: browser retains attended semantics for compatibility/human visual needs. Run prerequisite probe before expensive validation build; record exact missing tool/permission evidence and blocked verdict when neither permitted path works. Never silently substitute HTML, install tooling, expand permissions, or start more harness sessions; preserve shared two-session ceiling. Probe success is not proof of product behavior. Keep kit template and checked-in template byte-identical. Explain scripts are this repository's supported runner; generic kit consumers supply their own equivalent bounded runner.

## Units of work

### Unit A — bounded runner, spec routing and guidance
Files above plus tests/Muthur.Server.Tests/SpecGuardTests.cs. One implementer owns all changes; no parallel sessions. Build from the frozen spec branch task/T-94-headless-browser, measure against main. Commit work; no muthur, push, merge, or hub access. Run dotnet build, dotnet test and node --test tests/browser-capability.test.cjs. Automated Node tests use fake dependencies/process seams (no downloads or installed browser required) to cover available/unavailable paths, explicit bad overrides, path spaces, timeouts/cleanup, and unsafe smoke URL rejection. Orchestrator alone runs installed CLI and real browser end-to-end commands.

## Verification

needs: headless-browser

Preflight before build: pwsh -NoProfile -File scripts/browser-capability.ps1 -PlaywrightPath C:/WorkSrc/MUTHUR/.work/overseer-0919/browser/node_modules/playwright -BrowserPath "C:/Program Files/Google/Chrome/Application/chrome.exe". Exit 0 and successful real button interaction required. Repeat with -NodePath a-definitely-missing-node and with -PlaywrightPath C:/missing-T94-playwright; exit 2 with accurate prerequisites, no lingering child processes.

Run dotnet build; dotnet test; node --test tests/browser-capability.test.cjs. Require zero warnings/errors and all tests pass.

Install: pwsh -NoProfile -File scripts/install.ps1 -Destination ./artifacts/t94. Record live installed muthur status PID/startedAt first. Using only child-process env overrides MUTHUR_HOME=<absolute isolated .work/t94-scratch>, MUTHUR_URL=http://127.0.0.1:7494 and cleared MUTHUR_TOKEN/MUTHUR_AGENT, invoke artifacts/t94/muthur.exe up. Never use a build output CLI or modify live hub data. Run browser-capability.ps1 with the explicit module/browser paths above, -Url http://127.0.0.1:7494 -AgentName t94-browser-fixture. Confirm registration via installed scratch CLI agent list and live agent list excludes fixture. Stop scratch with installed CLI down in finally; confirm PID gone and live identity unchanged; remove scratch data after verifying absolute path containment. Save concise evidence in specs/T-94-evidence.md. Verify spec headless routing through integration tests (legacy browser stays attended). Summarize large output with muthur utility summarize before reading it, then verify cited evidence. Clean owned scratch processes before task implemented.

## Out of scope / follow-ups

Automatic remote capability matching and any prerequisite installation need separate authorization; unavailable tools are reported explicitly.
# T-94 review correction — frozen before correction delegation

The original spec's cross-origin redirect rejection is not met by route.continue(). The installed Playwright types document that the route handler is invoked only for the first URL of a redirect chain. A sandboxed real-browser check against two owned loopback servers produced one request at the cross-origin destination, then a Blazor timeout.

Required correction: in browser-smoke.cjs, fetch intercepted same-origin HTTP requests with maxRedirects: 0 and an explicit timeout within the runner's deadline. Reject every redirect response before it reaches the browser; the supported scratch /console interaction requires no redirects. Abort the intercepted route and record an explicit refused-redirect diagnostic. Fulfill nonredirect responses from the fetched response. Keep the existing initial cross-origin request guard. Do not use route.continue for allowed HTTP requests because it can follow unchecked redirects.

Add dependency-free regression tests that assert maxRedirects: 0, reject a redirect to a second origin before any continuation/fulfillment, and preserve nonredirect success and direct cross-origin abort. Also verify same-origin redirects fail explicitly (the smoke runner does not support redirects). Keep Chromium sandbox enabled and the parent total deadline intact. Run node --test tests/browser-capability.test.cjs. Do not run a browser or hub: the orchestrator reruns the real two-loopback-server regression and scratch UI smoke.

No other scope expansion. Report WORKTREE, BRANCH, commit, and checks. No muthur, push or merge.

Second verified correction: a hidden detached Node child inheriting stdout survives the Node parent's exit, and the PowerShell probe then exceeded six seconds despite TimeoutSeconds=2. The orchestrator's outer harness terminated the probe and recorded/cleaned the owned fixture child. The defect is synchronous ReadToEndAsync.GetResult after waiting only for the root PID.

The total deadline must cover root exit AND both redirected output tasks. On Windows, use kill-on-close job ownership so cleanup remains possible after the root exits. Reuse the checked-in standalone CensusWindowsProcess implementation from src/Muthur.Server/Services/CensusWindowsProcess.cs through a build-free Add-Type adapter: add the explicit standard usings and nullable context needed by Add-Type and expose the class to PowerShell in that transient compilation; do not modify the server source. This source already launches suspended, assigns to its job before resume, limits inherited handles, quotes arguments, and exposes Process/StandardOutput/StandardError/StopAsync/Dispose. It clears live identity variables. The runner is supported on Windows; on other platforms return a clear unavailable unsupported-platform reason rather than claiming an unimplemented process-ownership guarantee. Explain this Windows scope in runner guidance. Parent owns the job through finally and bounds stream waits by the same remaining deadline; dispose/stop owned job on success and failure, including early root exit. No package install or build needed for preflight.

Add a regression fixture that spawns a hidden detached Node child with inherited stdout/stderr, records its PID, and immediately exits its parent. Assert the PowerShell probe returns within a bounded outer watchdog, reports timeout when streams remain open, and the child PID is gone. The test's finally must clean the recorded child even if the assertion fails. Cover root still alive and root already exited. Use a sufficiently large initial deadline to allow Add-Type startup, and separate total watchdog. Preserve path-with-spaces and missing-tool checks. Run node tests; orchestrator reruns real available/unavailable probes and the detached-child regression before full build.
