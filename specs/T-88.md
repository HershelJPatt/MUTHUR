# T-88 — Attended application of protected agent definitions

> Frozen spec. Implement exactly this unit; stop and report missing decisions.

## Goal
Make founder request #41 operational through explicit documentation and task routing: application of harness-protected agent definitions stays attended. Source preparation remains possible unattended.

## Context
Founder answered request #41 on 2026-09-21: "Keep application of protected agent definitions attended; implement explicit documentation and task routing." That supersedes any inference from T-67 clearing its individual blocker. There are no prerequisite tasks for this documentation work. The current CLI already provides task attended, ask --kind human, and task dependencies. kit/core/orchestrate.md is expanded into all harness procedures; kit/core/implementer.md is expanded into worker instructions. README.md describes kit install. Existing KitInstallTests exercise expansion in disposable repositories.

## Non-goals
No permission expansion, installer bypass, allow rules, new CLI/API/runtime behavior, or actual application of installed agent definitions. Do not edit any .claude file, including installed agents or skills. Do not run kit install against this repository or any real harness configuration. Do not mark T-88 attended merely because it documents attended work.

## Design
Add a self-contained "Protected agent definitions" section to kit/core/orchestrate.md before Identity. State:
- Applying harness-protected installed agent definitions (including .claude/agents/*.md) requires an attended session and explicit founder approval for the reviewed change. This holds even for tightening rails.
- Agents may prepare, review and test source changes under kit/ without applying them to installed definitions. Source approval, validation, task landing, earlier one-off permission, and successful authentication do not grant standing application permission.
- Do not route around refusal using another harness, shell, copy command, installer, or changed allow rule. Stop the attempted application.
- Before delegation separate source-only work from installed application. If installed application is required by the task, record exact target paths, reviewed source commit/diff and verification in the spec/task evidence; run `muthur task attended T-n --reason "Protected agent definitions require founder-approved attended application"`. If explicit approval of that concrete application is missing, use `muthur ask "Approve attended application of the recorded protected agent-definition diff?" --task T-n --kind human` and exit. If approval already exists, keep the attended routing and checkpoint/exit for the attended session. No repeated request for an already approved exact change.
- Where source preparation can land independently, keep that task source-only; create a separate application task (`muthur task add "Apply reviewed protected agent definitions" --parent T-n --body "..."`, with real evidence replacing ellipsis), mark the application task attended immediately, and record source task as its prerequisite with `muthur task dependencies T-application --after T-n --reason "Apply only the landed reviewed source"`. A task genuinely blocked by that application uses dependencies on the application task and exits. Never release a blocked task into runnable backlog or ask the founder to poll it. These are example placeholders, not literal task IDs to execute.
- Clearing attended requires evidence that the attended application was completed or that installed application is no longer in scope; it does not authorize later unattended protected writes. Source-only testing must not be reported as installed application verification.
Add a short matching boundary bullet to kit/core/implementer.md: source edits are allowed within the frozen unit; stop/report blocked on required protected installed writes; never bypass; orchestrator owns routing and workers still never run muthur.
Add a short README paragraph immediately following kit install setup description: protected installed application requires explicit founder approval and attendance, kit install is not a sanctioned unattended bypass, link to kit/core/orchestrate.md#protected-agent-definitions for workflow.

## Units of work
### Unit A — Documentation and routing
- Files: kit/core/orchestrate.md, kit/core/implementer.md, README.md only.
- Depends on: nothing.
- Acceptance: all design clauses above are present, coherent with surrounding instructions, no installed .claude changes and no runtime changes. Preserve existing content except these focused additions.
- Base branch: task/T-88-attended-rails; default branch: main. Dispatch SHA supplied separately. Verify branch and SHA before writing.

## Verification
Worker and orchestrator run:
```
git diff --check
dotnet build --nologo
dotnet test tests/Muthur.Cli.Tests/Muthur.Cli.Tests.csproj --no-build --nologo
```
Build and CLI suite pass. Existing kit installation tests operate on disposable repositories, not live protected definitions. Review every changed line against Design; verify only the three named documentation files plus this spec/evidence changed relative to the frozen base. No new tests needed for prose; existing expansion tests cover distribution. No browser, GUI, scratch hub or actual protected-file application is required.

## Out of scope / follow-ups
No application task is needed for T-88: this unit changes only source procedures and README, not agent-definition source or installed definitions. Future tasks requiring installed changes follow the documented route.
