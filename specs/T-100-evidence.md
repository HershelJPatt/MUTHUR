# T-100 resumption evidence — 2026-09-21

## Subject and review
Product candidate: worker/T-100-smoke-readiness at 078716ee11caca9477ade3c0bc6a53fa5a2bce2d, to be integrated into task/T-100-capability-matching. The final evidence-only commit changes no product code. Claim/task/ledger were read before writes. No founder decision changes approved scope. T-113 is landed; T-94 and T-67/T-97 remain independent/unverified routes. No permissions, budgets, accounts or live configuration were changed.

The preserved branch was ahead of the old task head: Unit C 7157d19 was integrated instead of restarted. Every feature diff and each subsequent implementer diff was reviewed by the orchestrator. Unit D closed checkout helper/hook execution, reserved fixture/link containment and scratch process ownership. A harmless marker experiment proved clean filters can execute despite --no-ext-diff/--no-textconv; Unit E therefore guarded identity inspection as well as probes. Unit F moved executable metadata verification after owned-process readiness and attributed init/commit setup failures without changing execution budgets. Orchestrator wrote specs/evidence only, no product code.

## Independent checks
- Preserved Unit C: dotnet build passed, zero warnings/errors; dotnet test --no-build -v n passed 5,085, skipped 1, failed 0 (5,086 total). Server cleanup: 651 removed, zero kept/removal failures. Logs: .work-T100-resume-build.log and .work-T100-resume-tests.log.
- Integrated through Unit E, 5f6139e: independent build passed. A second full-suite command was run; two Launch fixture setup failures occurred under concurrent host load. One explicitly reports the 10-second Git setup budget expired; the other failed its fixture git add before the behavior under test. Preserve the red run, not a claim it was clean. Final totals are appended below. Logs: .work-T100-unitE-build.log and .work-T100-unitE-full-tests.log.
- Final product 078716e: independent dotnet build passed, zero warnings/errors (12.23s). All 61 Capability tests passed, including every new isolation/inspection regression, all four diagnostic cases and both cases from the broad-run failures. Command: dotnet test tests/Muthur.Launch.Tests/Muthur.Launch.Tests.csproj --no-build --filter FullyQualifiedName~Capability -v n. Logs in .worktrees/worker-T-100-smoke-readiness/.work-final-host-*.log. No timeout limits were expanded to get a pass. Worker F's four sandbox cleanup failures were not accepted as verification; these four passed on host.
- Native AOT installed product 078716e: scripts/install.ps1 -Destination ./artifacts/T-100-final -Ref 078716ee11caca9477ade3c0bc6a53fa5a2bce2d passed. Installed scratch script passed, revision checked, admission refusal/replay/release checked, unknown/stale/unavailable matching checked, one supported simulated worker completed. artifacts/T100-final-smoke.json has cleanupFailures=[] and confirms zero active reservations/services.
- No browser/GUI checks, permission changes, pushes, default-branch merge or live install performed.

## Measurement windows and limits
Final installed simulated cohort n=1, 2026-09-21 07:09:43.964–07:11:14.153 UTC, product 078716e: one simulated probe process, 16,994ms probe duration, one simulated full worker start, zero real model starts. These counters are distinct; do not sum them as successes or infer a speedup. Earlier preflight attempts at 507a756: two failures (commit fixture setup, then startup metadata race), followed by a passed traced run with 33,883ms simulated probe and one simulated worker. Those are separate attempts, not additional live successes. All reported scratch cleanup failures were empty.

Actual authorized installed-route cohort n=1 at product 078716e: see artifacts/T100-real-observation.json for exact UTC window and pinned tiny-repository revision. The existing live admission endpoint returned HTTP 404 through capability_probe_admission_failed. Zero probe/model starts; no production capability success or real pilot is claimed. Installed scratch admission works; the current live hub does not expose the required endpoint. No budget reset, privilege expansion, bypass or retry was attempted.

Implementation dispatches this resumption are separate from capability observations: Unit D completed in 696s; Unit E timed out at 481s with launcher-preserved cd96589; Unit F returned blocked after 271s with launcher-preserved 078716e. All were implementer-tier codex/gpt-6-astra. Launcher commits were reviewed. Worker reports, local summaries and host verification are not interchangeable evidence. The exact Unit D duration is recorded in .work-T100-isolation.json and supersedes any narrative estimate.

## Follow-up and recovery
T-118 depends on T-100 and owns the post-landing installed revision and first real capability-matched assignment within a bounded observation window. Missing real-use evidence, unavailable native Claude/browser routes and the broad-run fixture deadline failures remain explicit. Do not label a smaller number of starts a benefit without successful outcomes and a named cohort/window.

Recovery is documented in docs/capabilities.md: inspect the exact identity, remove only the selected stale cache record, explicitly re-probe through admission; never seed live success or expand permissions. An uncertain cleanup retains its reservation for explicit recovery. All owned scratch processes must be gone before implemented; final process inventory and broad-suite totals follow below.


## Final handoff — NOT IMPLEMENTED
At 2026-09-21 07:20 UTC the conductor 45-minute session window was closing. The second full suite had completed Core/CLI/XML projects and Launch (131 passed, 2 setup failures), while Server was still running (806 observed passes of 846 at 07:19:44). The owned dotnet test process 44760, started 07:03:16 and identified by parent command naming .work-T100-unitE-full-tests.log, was terminated with its descendants after binding its handle/start identity. This full run is INTERRUPTED, not passing. Do not mark implemented from this evidence alone.

Final product 078716ee11caca9477ade3c0bc6a53fa5a2bce2d is integrated and reviewed. No worker or scratch hub is left running. The final independent 61/61 Capability tests and installed scratch smoke passed. Remaining work: run the final complete suite with controlled project concurrency (for example dotnet test --no-build -v n -m:1 after a final build), diagnose any repeat fixture setup deadline failures without changing production budgets, and obtain a complete cleanup summary. Preserve red/interrupted logs. The first full baseline run was green, but is not substituted for this remaining gate. If T-117's independent admission-test race appears, set a real dependency rather than duplicate its fix. No current T-100 founder decision or unanswered question changes scope.

Then review exact branch/head and newest task decisions, clean owned processes, submit implemented and exit. Native AOT install at artifacts/T-100-final is product 078716e; simulated installed smoke artifacts/T100-final-smoke.json passed. Actual live admission observation artifacts/T100-real-observation.json returned HTTP 404 and zero starts; do not repeat a paid probe or bypass admission merely to manufacture success. T-118 already tracks post-landing installed revision and real assignment evidence.
