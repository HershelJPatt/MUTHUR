# T-66 — Document the CLI test project

> Frozen spec. If anything is wrong or missing, stop and report rather than improvise.

## Goal

Make CLAUDE.md's test-layout rule a complete map that directs CLI command wiring and query-string tests to the existing CLI test project.

## Context

The Tests bullet under Rules of the codebase lists Core, Launch and Server tests but omits tests/Muthur.Cli.Tests. ValidationListQueryTests.cs checks optional query filters and escaping; ReceiptsCommandTests.cs checks command wiring, option parsing and query construction. The omission misdirected a prior frozen spec.

## Non-goals

No product code, new tests, test counts, historical spec edits, or unrelated documentation changes.

## Design

In CLAUDE.md's existing Tests bullet, insert the following clause immediately after the Core.Tests clause and before Launch.Tests:

`tests/Muthur.Cli.Tests` for CLI command wiring and query-string construction in the Native AOT pass-through HTTP client (before requests reach the hub);

Preserve the other clauses and the sentence "New behavior comes with tests." exactly. Change no other text in CLAUDE.md.

## Units of work

### Unit A — Correct the test-layout rule
- **Files:** CLAUDE.md only.
- **Does:** the exact insertion above.
- **Depends on:** nothing.
- **Acceptance:** the documented path exists, the clause matches the existing tests, and all verification below passes. Commit the change.

## Verification

Run from the task worktree in PowerShell, unattended:

```powershell
git diff --check main...HEAD
dotnet build --disable-build-servers
dotnet test --no-build --blame-hang-timeout 2m
```

All commands must exit zero. Review `git diff main...HEAD -- CLAUDE.md`: only the specified insertion is permitted. Confirm `Test-Path tests/Muthur.Cli.Tests/Muthur.Cli.Tests.csproj` returns True. Read ValidationListQueryTests.cs and ReceiptsCommandTests.cs to verify the description. No scratch hub, browser or development CLI is needed for this documentation change.

## Out of scope / follow-ups

None.
