# T-103 read-only delegation-family grouping proposal

Observed 2026-09-21 approximately 04:40–04:44 UTC; source main cb52ed1, live hub task reports T-67/T-74/T-79/T-81/T-85/T-86/T-88/T-97. Eight task records inspected; five independent historical report bodies compared (T-74/79/81/85/86), not five new experiments. No live incident links, cancellations, suppression or permissions changes made. Utility summaries were advisory; task bodies and cited request events checked directly.

Potential incident A: Claude Agent-tool subagent verification/commit command permission denials in unattended worktrees. T-74 records two probes and PowerShell denial text; T-79 retains T-61's blocked build/commit report; T-85 retains T-57's distinct specialist attempts and unverified edits. These three share a reported PowerShell denial signature but have different sessions/configuration evidence. T-81/T-86 preserve T-63's repeated failure history, including differing shell descriptions. Link only as individual observations after checking tuple/configuration; do not claim byte-exact equal configurations or a proven shared root cause. T-81's Bash description is not silently rewritten to PowerShell. T-86's inheritance explanation is a reported diagnosis, not independently measured here.

T-67 is the related repair/probe task, not evidence that recovery succeeded. Event 3719/request 32 explicitly reports build 0 warnings/errors and tests 4438 passed, zero failed/skipped, but actual Claude probe could not start because OAuth refresh failed and account was limited. The utility summary incorrectly described four failing tests from older history; the later source request was checked and takes precedence for that run. No inference of permission behavior from a probe that never started.

Potential incident B: expired Claude authentication/account availability, retained separately from command denials. Its wake condition is usable authorized Claude account plus the bounded required probe, never a Codex substitute. T-97 requires post-T-67 real Agent-tool permission measurement under founder's normal permission mode, per T-87 decision 33 referenced by T-97; keep its T-67 dependency.

Separate policy context C: protected .claude/agents application requires attendance. T-88 event 5426, founder answer 41: "Keep application of protected agent definitions attended; implement explicit documentation and task routing." Do not group this as an incidental command-denial defect or clear it through an incident recovery record.

Known workaround evidence: T-81/T-85/T-86 report use of muthur worker run. T-103's explicit instruction authorizes implementer-tier delegation and disables local implementation. This permits T-103's own worker path; it does not authorize new accounts, grants or other task permission changes.

Proposed live rollout after installed-product verification and landing: use incident match/show as read-only triage; retain each evidence reference; create suspected grouping with explicit configuration unknown where unknown; no suppression until a current exact condition and assignment are demonstrated. Reversal is observation unlink; recovery requires new attributable evidence. Do not mark the Claude incident resolved while T-67/T-97 remain outstanding.

Measurement limitations: no new Claude process starts, worker runs or recovery successes in this historical review. Actual diagnosis-session counts, mistaken-grouping rate, false-suppression rate and recovery-to-resume delay are unknown. Source report counts are not independent performance samples and support no speedup claim.
