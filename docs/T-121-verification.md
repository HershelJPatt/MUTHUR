# T-121 implementation verification

The resumed owner session continued `task/T-121-worker-admission` from product commit `d5ad15c36cb33633f753e041cd4e659ba7a82d47`. The branch contained the completed admission implementation beyond the ledger's last implementation note. Founder request #53 authorized this already-admitted session to finish directly, without implementation children. Independent validation and landing remain conductor responsibilities.

Review covered the frozen spec, all implementation changes, shared scheduling and daily accounting, replay and original-owner recovery, CLI admission before setup, sequential fallback, Windows job containment, and the synthetic verification script. Existing ceilings, daily budgets, account limits, reviewer eligibility and probe APIs are preserved. T-108 remains dependent on this repair and a verified supported installed route; this verification is not evidence of real routing quality.

The resumed build (`scripts/check-t121.ps1 -Check build`) passed with zero warnings and zero errors. The existing isolated AOT installation identifies its CLI as `0.1.0+d5ad15c36cb33633f753e041cd4e659ba7a82d47`. Running `scripts/verify-t121.ps1` against that installation on 2026-09-21 passed all 41 assertions with zero cleanup failures. Evidence: `artifacts/t121-resumed-installed-evidence.json` in the implementation worktree.

Installed assertions cover legacy specs, missing-task refusal before worktree creation, two synthetic fallback attempts with separate reservations, confirmed process cleanup and release, shared worker/probe capacity, daily and account denial, input conflict, non-executing replay, hub restart retention, explicit idempotent recovery, and original CLI death. Forced CLI death terminated its synthetic worker through job containment while preserving the durable reservation. The scratch hub, repository and synthetic processes were cleaned by the verifier.

The initial resumed full-suite run completed with 5,238 passed, one failed and one skipped. The failure was a timeout in the existing capability-probe fixture's setup `git commit` (exit 124). All 950 server tests passed, including the earlier session's failing task-unit test; test cleanup reported 732 hubs removed, zero retained and zero unremovable. The skip was the census unsupported-platform test, which is inapplicable on Windows. A bounded full-suite rerun uses `scripts/check-t121.ps1 -Check test -Serialized`, preserving all assertions and deadlines while running one project at a time with four processors exposed to the test runtime. This is a local test scheduling option, not a MUTHUR capacity or budget change.

The installed verifier also now terminates and waits on its exact synthetic process handle in `finally` if the CLI-death assertion fails, so failed verification does not leave that process for an operator to clean up. Re-running the revised verifier at 18:27–18:28 UTC passed all 41 assertions again with zero cleanup failures (`artifacts/t121-final-installed-evidence.json`). Product sources and tests are unchanged from the installed revision; the resumption changes only verification scripts and documentation.

Final full-suite coverage completed across successful project runs: CLI 324 passed, Core 3,742 passed, Launch 159 passed, Server 950 passed with one Windows-inapplicable skip, and XML checks 64 passed. Total: **5,239 passed, one skipped, zero remaining test failures**. The serialized solution command reached its 20-minute outer limit after the server suite and its cleanup completed, while compiling the final XML-check project; that command is not claimed as an exit-zero run. The remaining project completed separately with exit zero using `scripts/check-t121.ps1 -Check test -Project tests/Muthur.XmlDocCheck.Tests/Muthur.XmlDocCheck.Tests.csproj -Serialized`. Its build had zero warnings and errors. The earlier probe fixture timeout passed in the serialized launch suite.

The serialized server run reported 732 test hubs removed, zero retained, and zero unremovable. Both installed verifier runs reported no cleanup failures. Completed logs are retained under `artifacts/t121-checks/` in the implementation worktree; `t121-test-serialized.log` records the completed first four projects and `t121-test-project-serialized.log` records the final XML project. Full-log utility summaries were treated as advisory and checked against the exact test totals and failure lines. One final summary attempt encountered the shared inference lock; original evidence was used without retry.
